buildscript {
    repositories {
        maven {url = "https://files.minecraftforge.net/maven"}
        maven {url = "https://repo.spongepowered.org/repository/maven-public"}
    }

    dependencies {
        classpath("net.minecraftforge.gradle:ForgeGradle:latest.release")
        classpath("org.spongepowered:mixingradle:0.7-SNAPSHOT")
    }
}

plugins {
    id("java-library")
    id("maven-publish")
}

plugins.apply("net.minecraftforge.gradle")
plugins.apply("org.spongepowered.mixin")

group = "user11681"
version = "3.0.0"

repositories {
    mavenCentral()

    maven {url = "https://auoeke.jfrog.io/artifactory/maven"}
    maven {url = "https://maven.tterrag.com"}
}

dependencies {
    minecraft("net.minecraftforge:forge:1.16.5-36.1.23")

    api("user11681:reflect:latest.release")
}

minecraft {
    mappings("official", "1.16.5")
}
